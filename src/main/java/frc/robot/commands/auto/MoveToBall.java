package frc.robot.commands.auto;

import frc.robot.subsystems.DriveSystem;
import frc.robot.subsystems.FieldPositioningSystem;
import frc.robot.subsystems.IntakeSystem;

public class MoveToBall {
    public MoveToBall (DriveSystem driveSystem, FieldPositioningSystem fieldPositioningSystem, int startPosition) {
        double[] desiredPos = new double[2];
        switch (startPosition) {
            //implement

        }
        // new MoveToPosition(driveSystem, fieldPositioningSystem, desiredPos);

    }
    
}
